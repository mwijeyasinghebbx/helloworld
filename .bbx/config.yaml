runners:
 ubuntu-runner:
  image: ubuntu
 centos-runner:
  image: centos
  
jobs:   
 hello-world:
  resource_spec: micro
  runner: centos-runner
  steps:
   - run:
      name: Say Hello World
      command: |
       echo "Hello World" 
workflows:
 hello-world-workflow:
  jobs:
   - hello-world
